# Expo SDK 57 — docs first

Read https://docs.expo.dev/versions/v57.0.0/ before changing Expo/React Native APIs.

This repo follows hub-style architecture. Read `.cursor/skills/mealprep-architecture/SKILL.md` before adding screens.

- Mobile only (`ios` + `android`). Do not add Expo web.
- Products: local JSON in `src/data/`, no API.
- Figma: use the Figma MCP + `.cursor/skills/figma-to-native/SKILL.md`.
